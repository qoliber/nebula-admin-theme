<?php
/**
 * Nebula Confirm Dialog — global singleton, included once via root.phtml.
 *
 * State lives in Alpine.store('nebulaConfirm'). The JS API
 * window.Nebula.confirm({...}) is the only entrypoint; this file is purely
 * presentation. See ts/confirm.ts for the state machine.
 *
 * @var \Magento\Framework\View\Element\Template $block
 */
?>
<div
    x-data
    x-cloak
    x-show="$store.nebulaConfirm.open"
    x-transition.opacity.duration.150ms
    x-on:keydown.escape.window="$store.nebulaConfirm.cancel()"
    class="fixed inset-0 z-[2147483646] flex items-center justify-center bg-gray-900/40 p-4"
    style="display: none;"
    @click.self="$store.nebulaConfirm.cancel()"
>
    <div
        x-show="$store.nebulaConfirm.open"
        x-transition:enter="transition ease-out duration-200"
        x-transition:enter-start="opacity-0 scale-95"
        x-transition:enter-end="opacity-100 scale-100"
        role="dialog"
        aria-modal="true"
        aria-labelledby="nebula-confirm-title"
        class="w-full max-w-md rounded-lg bg-white shadow-xl ring-1 ring-gray-200"
    >
        <div class="px-6 pt-6 pb-4">
            <div class="flex items-start gap-4">
                <div
                    class="flex h-10 w-10 shrink-0 items-center justify-center rounded-full"
                    :class="$store.nebulaConfirm.options?.danger ? 'bg-red-100 text-red-600' : 'bg-indigo-100 text-indigo-600'"
                >
                    <svg x-show="$store.nebulaConfirm.options?.danger" class="h-5 w-5" xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" stroke-width="2" stroke="currentColor"><path stroke-linecap="round" stroke-linejoin="round" d="M12 9v3.75m-9.303 3.376c-.866 1.5.217 3.374 1.948 3.374h14.71c1.73 0 2.813-1.874 1.948-3.374L13.949 3.378c-.866-1.5-3.032-1.5-3.898 0L2.697 16.126ZM12 15.75h.007v.008H12v-.008Z"/></svg>
                    <svg x-show="!$store.nebulaConfirm.options?.danger" class="h-5 w-5" xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" stroke-width="2" stroke="currentColor"><path stroke-linecap="round" stroke-linejoin="round" d="M11.25 11.25l.041-.02a.75.75 0 011.063.852l-.708 2.836a.75.75 0 001.063.853l.041-.021M21 12a9 9 0 11-18 0 9 9 0 0118 0zm-9-3.75h.008v.008H12V8.25z"/></svg>
                </div>
                <div class="min-w-0 flex-1">
                    <h2 id="nebula-confirm-title" class="text-base font-semibold text-gray-900" x-text="$store.nebulaConfirm.options?.title"></h2>
                    <p class="mt-2 text-sm text-gray-600" x-text="$store.nebulaConfirm.options?.message"></p>
                </div>
            </div>
        </div>
        <div class="flex justify-end gap-3 rounded-b-lg bg-gray-50 px-6 py-3">
            <button
                type="button"
                data-nebula-confirm-cancel
                @click="$store.nebulaConfirm.cancel()"
                class="inline-flex items-center rounded-lg bg-white px-4 py-2 text-sm font-semibold text-gray-700 shadow-sm border border-gray-300 transition hover:bg-gray-50"
                x-text="$store.nebulaConfirm.options?.cancelText ?? 'Cancel'"
            ></button>
            <button
                type="button"
                data-nebula-confirm-confirm
                @click="$store.nebulaConfirm.confirm()"
                class="inline-flex items-center rounded-lg px-4 py-2 text-sm font-semibold text-white shadow-sm transition"
                :class="$store.nebulaConfirm.options?.danger ? 'bg-red-600 hover:bg-red-500' : 'bg-indigo-600 hover:bg-indigo-500'"
                x-text="$store.nebulaConfirm.options?.confirmText ?? 'Confirm'"
            ></button>
        </div>
    </div>
</div>
