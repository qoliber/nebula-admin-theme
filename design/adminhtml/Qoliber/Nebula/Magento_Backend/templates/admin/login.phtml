<?php
/**
 * Qoliber Nebula — Admin Login Form (right side)
 *
 * @var \Magento\Framework\View\Element\AbstractBlock $block
 */
?>
<div class="flex min-h-full flex-col justify-center px-8 py-12 lg:px-16"
     x-data="loginForm()">

    <div class="w-full max-w-sm mx-auto">
        <h2 class="text-2xl font-bold tracking-tight text-gray-900">
            <?= $block->escapeHtml(__('Welcome back')) ?>
        </h2>
        <p class="mt-2 text-sm text-gray-500">
            <?= $block->escapeHtml(__('Sign in to your admin account')) ?>
        </p>
    </div>

    <?= $block->getChildHtml('messages') ?>

    <div class="w-full max-w-sm mx-auto">
        <form method="post"
              action=""
              id="login-form"
              autocomplete="off"
              @submit="onSubmit"
              class="space-y-5">

            <input name="form_key" type="hidden" value="<?= $block->escapeHtmlAttr($block->getFormKey()) ?>"/>

            <div>
                <label for="username" class="block text-sm font-medium text-gray-700">
                    <?= $block->escapeHtml(__('Username')) ?>
                </label>
                <input id="username"
                       type="text"
                       name="login[username]"
                       autofocus
                       required
                       x-model="username"
                       placeholder="<?= $block->escapeHtmlAttr(__('Enter your username')) ?>"
                       autocomplete="off"
                       class="mt-1.5 block w-full rounded-lg border border-gray-300 px-4 py-2.5 text-gray-900 shadow-sm placeholder:text-gray-400 focus:border-nebula-500 focus:ring-2 focus:ring-nebula-500/20 focus:outline-none transition sm:text-sm"/>
            </div>

            <div>
                <label for="login" class="block text-sm font-medium text-gray-700">
                    <?= $block->escapeHtml(__('Password')) ?>
                </label>
                <input id="login"
                       type="password"
                       name="login[password]"
                       required
                       x-model="password"
                       placeholder="<?= $block->escapeHtmlAttr(__('Enter your password')) ?>"
                       autocomplete="off"
                       class="mt-1.5 block w-full rounded-lg border border-gray-300 px-4 py-2.5 text-gray-900 shadow-sm placeholder:text-gray-400 focus:border-nebula-500 focus:ring-2 focus:ring-nebula-500/20 focus:outline-none transition sm:text-sm"/>
            </div>

            <?= $block->getChildHtml('form.additional.info') ?>

            <div class="pt-2">
                <button type="submit"
                        :disabled="loading"
                        class="flex w-full justify-center items-center rounded-lg bg-nebula-600 px-4 py-2.5 text-sm font-semibold text-white shadow-sm hover:bg-nebula-500 focus:outline-none focus:ring-2 focus:ring-nebula-500 focus:ring-offset-2 transition disabled:opacity-50 disabled:cursor-not-allowed">
                    <svg x-show="loading" x-cloak class="animate-spin -ml-1 mr-2 h-4 w-4 text-white" xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24">
                        <circle class="opacity-25" cx="12" cy="12" r="10" stroke="currentColor" stroke-width="4"></circle>
                        <path class="opacity-75" fill="currentColor" d="M4 12a8 8 0 018-8V0C5.373 0 0 5.373 0 12h4z"></path>
                    </svg>
                    <span x-text="loading ? '<?= $block->escapeJs(__('Signing in...')) ?>' : '<?= $block->escapeJs(__('Sign in')) ?>'"></span>
                </button>
            </div>

            <?= $block->getChildHtml('form.buttons') ?>
        </form>
    </div>
</div>

<script>
    function loginForm() {
        return {
            username: '',
            password: '',
            loading: false,
            onSubmit() {
                if (this.username && this.password) {
                    this.loading = true;
                }
            }
        }
    }
</script>
