<?php

/**
 * Hidden field — still routed through the Alpine model layer. Emits no
 * visible markup; the nebulaField_hidden component owns the value and
 * serializes on submit.
 *
 * @var \Qoliber\NebulaForm\Block\Form $block
 */
$config = json_encode([
    'fieldName' => $fieldName,
    'value' => (string) ($fieldValue ?? $field['default'] ?? ''),
], JSON_UNESCAPED_SLASHES);
?>
<div x-data="nebulaField_hidden(<?= /* @noEscape */ htmlspecialchars((string) $config, ENT_QUOTES, 'UTF-8') ?>)" style="display:none"></div>
