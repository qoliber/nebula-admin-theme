<?php

/** @var \Qoliber\NebulaForm\Block\Form $block */
$config = json_encode([
    'fieldName' => $fieldName,
    'value' => (string) ($fieldValue ?: '#000000'),
    'disabled' => $isDisabled,
    'validation' => $field['validation'] ?? [],
], JSON_UNESCAPED_SLASHES);
$fieldControl = '<div x-data="nebulaField_color(' . htmlspecialchars((string) $config, ENT_QUOTES, 'UTF-8') . ')">'
    . '<input type="color"'
    . ' id="' . $escaper->escapeHtmlAttr($fieldId) . '"'
    . ' x-model="value"'
    . ($isDisabled ? ' disabled' : '')
    . ' class="h-10 w-16 cursor-pointer rounded-lg border-0 p-1 shadow-sm">'
    . '</div>';
include __DIR__ . '/_row.phtml';
